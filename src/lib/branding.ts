/**
 * Swap these for your own league's branding. LEAGUE_NAME can also be
 * overridden via the NEXT_PUBLIC_LEAGUE_NAME env var without touching code.
 */
export const LEAGUE_NAME = process.env.NEXT_PUBLIC_LEAGUE_NAME?.trim() || 'Oakwood Fantasy Football';
export const LEAGUE_TAGLINE = 'Stats, standings, and receipts for the whole league';
export const LEAGUE_EMOJI = '🏆';
